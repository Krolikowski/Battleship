from setuptools import setup, find_packages
from os.path import join, dirname

setup(
    name='battleship',
    version='0.1b',
    packages=find_packages(),
    long_description="Battleship Game with two text-frontends and a GUI demo",
)
